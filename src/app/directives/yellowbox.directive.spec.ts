import { YellowboxDirective } from './yellowbox.directive';

describe('YellowboxDirective', () => {
  it('should create an instance', () => {
    const directive = new YellowboxDirective();
    expect(directive).toBeTruthy();
  });
});
